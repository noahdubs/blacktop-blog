<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"> 
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"> 
    <link rel="stylesheet" href="/stylesheets/app.css">

    <title>Test Site</title>
</head>
<body>
    <nav class="navbar navbar-expand-md navbar-dark bright">
        <a href="/home" class="navbar-brand light">PostMania</a>
        <button class="navbar-toggler border" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon toggler"></span>
        </button> 
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            
            <ul class="navbar-nav mr-auto"></ul>
            <ul class="navbar-nav navbar-right l-space">
                <!-- Home -->
                <li class="nav-item">
                    <a href="/home" class="nav-link light">Home</a>
                </li>
            </ul>
            <ul class="navbar-nav navbar-right l-space">
                <!-- About -->
                <li class="nav-item">
                    <a href="/about" class="nav-link light">About</a>
                </li>
            </ul>
            <ul class="navbar-nav navbar-right l-space">
                <!-- Login/Profile -->        
                <li class="nav-item">
                    <% if(currentUser){ %>
                        <a href="<%= currentUser.username %>" class="nav-link light">Profile</a>
                    <% } else { %>
                        <a href="/login" class="nav-link light">Login</a>
                    <% } %>
                </li>
            </ul>
            <!-- Logout/ Register -->
            <ul class="navbar-nav l-space">
                <li class="nav-item">
                    <% if(currentUser){ %>
                        <a href="/logout" class="nav-link light">Logout</a>
                    <% } else { %>
                        <a href="/register" class="nav-link light">Register</a>     
                    <% }%> 
                </li>
            </ul>
        </div>
    </nav>

    <div class="container">
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>
    </div>


