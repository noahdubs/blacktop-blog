<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://fonts.googleapis.com/css?family=Martel+Sans&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"> 
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.css" type="text/css"> 
    <link rel="stylesheet" href="/stylesheets/app.css">

    <title>Blacktop Blog</title>
</head>
<body>
    <nav class="navbar fixed-top navbar-expand-md navbar-dark navbar-custom">
        <a href="/home" class="navbar-brand logo" id="brand"><i class="fas fa-road"></i> Blacktop Blog</a>
        <button class="navbar-toggler border" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon toggler"></span>
        </button> 
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            
            <ul class="navbar-nav mr-auto"></ul>
            <ul class="navbar-nav navbar-right l-space">
                <!-- Home -->
                <li class="nav-item">
                    <a href="/home" class="nav-link light"><i class="fas fa-home"></i> Home</a>
                </li>
            </ul>
            <% if(currentUser){ %>
                <ul class="navbar-nav navbar-right l-space">
                    <!-- About -->
                    <li class="nav-item">
                        <a href="/<%= currentUser.username %>/post/new" class="nav-link light"><i class="fas fa-edit"></i> New Post</a>
                    </li>
                </ul>
            <% } %>
            <ul class="navbar-nav navbar-right l-space">
                <!-- Login/Profile -->        
                <li class="nav-item">
                    <% if(currentUser){ %>
                        <a href="/<%= currentUser.username %>" class="nav-link light"><i class="fas fa-user"></i> Profile</a>
                    <% } else { %>
                        <a href="/login" class="nav-link light"><i class="fas fa-sign-in-alt"></i> Login</a>
                    <% } %>
                </li>
            </ul>
            <!-- Logout/ Register -->
            <ul class="navbar-nav l-space">
                <li class="nav-item">
                    <% if(currentUser){ %>
                        <a href="/logout" class="nav-link light"><i class="fas fa-sign-out-alt"></i> Logout</a>
                    <% } else { %>
                        <a href="/register" class="nav-link light"><i class="fas fa-user-plus"></i> Register</a>     
                    <% }%> 
                </li>
            </ul>
        </div>
    </nav>

    <div class="container">
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>
    </div>


